<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EmployeeManagementSystem.Pages.AssignTaskPage"
             Title="AssignTaskPage">
    
    <ScrollView Orientation="Horizontal">
        <StackLayout>
            <Label Text="Assign Task Page" FontSize="24" HorizontalOptions="Center"/>
            <CollectionView ItemsSource="{Binding Tasks}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Orientation="Horizontal" Padding="10">
                            <Label Text="{Binding Id}" WidthRequest="50"/>
                            <Label Text="{Binding EmployeeId}" WidthRequest="100"/>
                            <Label Text="{Binding TaskDescription}" WidthRequest="200"/>
                            <Label Text="{Binding Status}" WidthRequest="100"/>
                            <Label Text="{Binding Deadline}" WidthRequest="150"/>
                            <Label Text="{Binding CompletedDate}" WidthRequest="150"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ScrollView>
</ContentPage>